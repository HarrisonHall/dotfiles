# tmux general
set -g history-limit 10000
set -g mouse on
set -sg escape-time 0
set -g renumber-windows on
set -g default-command fish
# set -g default-shell fish

# Rebing window split
bind-key | split-window -h -c "#{pane_current_path}"
bind-key \\ split-window -h -c "#{pane_current_path}"
bind-key - split-window -v -c "#{pane_current_path}"
bind-key c new-window -c "#{pane_current_path}"

# Bind additional pane resize & movement
bind-key k select-pane -U 
bind-key j select-pane -D 
bind-key h select-pane -L 
bind-key l select-pane -R 
bind-key -r M-j resize-pane -D 5
bind-key -r M-k resize-pane -U 5
bind-key -r M-h resize-pane -L 5
bind-key -r M-l resize-pane -R 5
bind-key -r C-h swap-window -t -1\; select-window -t -1
bind-key -r C-l swap-window -t +1\; select-window -t +1

# Rebind tmux config reset
bind r source-file ~/.config/tmux/tmux.conf

# Rebind prefix (emacs)
set -g prefix C-q
unbind-key C-b
bind-key C-q send-prefix

# Start counting at 1
set -g base-index 1
setw -g pane-base-index 1

# Don't let scroll reset buffer
unbind -T copy-mode MouseDragEnd1Pane

# Theming
set -g default-terminal "xterm-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Status bar (home)
set -g status-position top

set -g @catppuccin_flavour "mocha" # or frappe, latte, macchiato, mocha
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_text "#W"
# set -g @catppuccin_window_separator ""
# set -g @catppuccin_window_right_separator "█ "
set -g @catppuccin_window_separator ""
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
# set -g @catppuccin_window_separator "  "
# set -g @catppuccin_window_left_separator ""
# set -g @catppuccin_window_right_separator ""
set -g @catppuccin_window_number_position "left"
set -g @catppuccin_window_middle_separator " "
set -g @catppuccin_window_default_fill "none"
# set -g @catppuccin_window_current_fill "all"  # number
set -g @catppuccin_window_default_fill "bracket-none"
set -g @catppuccin_window_current_fill "bracket"
set -g @catppuccin_status_modules_right "disabled directory session"
set -g @catppuccin_status_left_separator " / "
set -g @catppuccin_status_right_separator "█"
set -g @catppuccin_status_background "#181825"
set -g @catppuccin_window_status_enable "no"
set -g @catppuccin_pane_border_style ""
set -g @catppuccin_pane_active_border_style ""

# set -g @tmux2k-theme 'catppuccin'
# set -g @tmux2k-icons-only true
# set -g @tmux2k-show-powerline false

# # Define multiple groups
# set -g @tmux2k-group1-plugins "cpu ram"
# set -g @tmux2k-group1-delimiter " / "

# set -g @tmux2k-group2-plugins "battery network"
# set -g @tmux2k-group2-delimiter " / "

# set -g @tmux2k-group3-plugins "time"

# # Use them in your status bar
# set -g @tmux2k-left-plugins "none"
# set -g @tmux2k-right-plugins "group1 group2 group3 suspend session"

# set -g @tmux2k-window-list-alignment "left"
# set -g @tmux2k-window-list-left-sep "A"
# set -g @tmux2k-window-list-right-sep "B"

# set -g @tmux2k-group1-colors "catppuccin text"
# set -g @tmux2k-group2-colors "catppuccin text"
# set -g @tmux2k-group3-colors "catppuccin text"


# List of plugins
## Base
run ~/.config/tmux/plugins/tmux-sensible/sensible.tmux
## Theme
run ~/.config/tmux/plugins/catppuccin/catppuccin.tmux
# run ~/.config/tmux/plugins/tmux2k/2k.tmux
## Nesting
run ~/.config/tmux/plugins/tmux-suspend/suspend.tmux
